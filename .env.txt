# ============================================================
# JARVIS — Remote Terminal Controller
# Copia este archivo a .env y rellena los valores
# ============================================================

# --- Dashboard ---
DASHBOARD_PORT=3000
DASHBOARD_PASSWORD=jarvis2024
JWT_SECRET=jarvis_super_secret_key_for_jwt_token_signing_at_least_32chars

# --- Base de datos ---
DB_PATH=./data/jarvis.db

# --- WhatsApp ---
WA_AUTH_PATH=./data/whatsapp-auth
# 'qr' para escanear QR, 'code' para código de emparejamiento
WA_AUTH_TYPE=qr
# Solo si WA_AUTH_TYPE=code: número con código de país sin + (ej: 5215512345678)
WA_PHONE_NUMBER=

# --- Telegram ---
# Token del bot de Telegram (desde @BotFather)
TELEGRAM_BOT_TOKEN=8989525132:AAHGIBxQr0XgUs-kbK0NavE-27EiqNS_DDo

# --- Terminal ---
# Directorio de trabajo por defecto para ejecutar comandos
DEFAULT_CWD=C:\Users\USER
# Timeout máximo en segundos para un comando (0 = sin límite)
COMMAND_TIMEOUT_SECONDS=60
# Máximo de comandos por minuto por operador
RATE_LIMIT_PER_MINUTE=10

# --- Modo de operación inicial ---
# Opciones: ai | restricted | full-shell
INITIAL_MODE=ai

# --- Logging ---
LOG_LEVEL=info

# --- Seguridad ---
# IDs de dispositivos móviles autorizados (UUIDs separados por coma).
# Si está vacío, la validación de dispositivo está desactivada.
ALLOWED_DEVICE_IDS=1344eb4b-9e65-4dc1-aea9-31b007ff9720

# Orígenes permitidos para CORS (separados por coma)
CORS_ORIGINS=http://localhost:3000
